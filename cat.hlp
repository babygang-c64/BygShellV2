#cat <files> [-options]

:Print file(s) on screen.

*Options:
- n : # all lines
- b : # non-empty lines
- e : add $ at line end
- p : paginate
- h : hexdump
- a : PRG start addr for hex

=Examples:
 *cat -hp edit.prg
 *cat -n *.asm > dump.lst
 *cat error >> log
