#Small text editor

:edit [<file>] [options]

* Options
- A : ASCII input file, convert to PETSCII

* File management 
-
 run/stop : quit without saving
 ctrl-x : quit and save
 ctrl-s : save file

* Editor
-
 ctrl-a   : start of line
 ctrl-e   : end of line
 ctrl-w   : next word
 ctrl-k t : top of document
 ctrl-k e : end of document
 ctrl-k + : set mark
 ctrl-k - : goto mark
