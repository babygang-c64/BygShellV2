#menu [item of file pattern] [-options]

:Creates a menu combining items and
:filenames, provides menu to user 
:and return user choice in SH% and SH$
:SH% contains item position, 0 if none
:SH$ contains item content
:Items in quotes are a single item

*Options:
- l : show menu on left of screen instead
-     of right
- d : define default selected item
- k : use first letter of items to
-     navigate

=Examples:
 *menu *.asm
 *menu Cancel OK:? sh%
 *menu one two three four -d=2
