#cat <files> [-options]

:Print file(s) on screen.

*Options:
- n : # all lines
- b : # non-empty lines
- e : add $ at line end
- p : paginate
- h : hexdump
- s : PRG start addr for hex
- a = Convert ASCII file

=Examples:
 *cat -hpa edit.prg
 *cat -n *.asm > dump.lst
 *cat error >> log
